<!DOCTYPE html>
<html>
    <head>
        <title>{% block title '' %} - Twitter Application</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" type="text/javascript"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <script src="{{ asset('js/main.js') }}" type="text/javascript"></script>
            
        <style>
            body{
            text-align: center;
            }
        </style>

    </head>

    <body>
        <div class="container">
            <form action="/logout">
                <button name="btnSubmit" id="btnSubmit"  type="submit" class="btn btn-primary pull-left"  value="logout">Logout</button>
            </form>
            <h2>myproject</h2>           
            <img src="img/twitter.png" alt="twitter" width="250" height="250">
            <div class="navbar-form navbar-right">                   
                <div  style="position:absolute; top: 30px;width: 400px; margin-left: -300px;">
                    <form  action="/tweet" method="post">
                        <textarea id="textarea" onkeyup="limitText()" name="chars" maxlength="140" style="width: 85%"></textarea><br />
                        <span id="charcount_text"></span>
                        <button id="tweet"  type="submit" class="btn btn-primary pull-right"  value="tweet">Tweet</button>       
                    </form>
                </div>
            </div>
        </div> 
        <div class="tweets">
            {% for post in tweets %}
                <form  action="/delete?id={{post.id}}" method="post">
                    <div class="media">
                        <a class="media-left">
                            <img alt="" class="media-object img-rounded" src="{{ asset('img/user.png') }}" width="100", height="100">
                        </a>
                        <div class="media-body">
                            <h4 class="media-heading">@{{ post.userid  }} <span class="well-sm date">{{ post.created|date("M d")   }}</h4>    
                            <p>{{ post.post }}</p>
                            <button type="submit" class="btn btn-primary"  value="delete">Delete</button>                                            
                        </div>
                    </div>
                </form>
            {% endfor %}
        </div>

    </body>
</html>